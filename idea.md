frontend - react vite jsx (netlify)
backend - node express js (render)
database - mongodb

sOuLTEXTit website
AI powered text editor/canvas

admin view : 

admin panel (api keys management, models management), there will be two types of api keys and models (gemini api keys and models, groq api keys and models). three types of models will be there (text generation, image generation, speech to text), speech to text models will be used for voice typing, admin will be able to manage all of these. (is admin/not will be managed from mongodb) both admin and user can login/signup using email/password, google.

user view : 

the user will login/sign up -> they will see a wonderful customizable canvas/text editor where they can select which model to use for the ai, but api keys will auto-switch to the next one if faced any error. user will write or paste anything on the canvas, user can choose to select a portion of the document or the full document and based on that user can write prompts to the user. -> user should be able to do anything, ask anything, the ai should be able to do anything any change to the document. before doing anything the ai will show the diff/changes and then if the user allows it then only it will be applied to the editor/canvas.
there will be canvas history. user should be able to export/download the canvas as png/pdf/txt/markdown/etc. user should be able to import canvas as well.
there will be voice typing button which will use the speech to text models.
user and admin both should be able to configure their choice of models for each job in settings

--------

send me all the codes in full which i can just copy and paste.
send me all the instructions also about all the steps.